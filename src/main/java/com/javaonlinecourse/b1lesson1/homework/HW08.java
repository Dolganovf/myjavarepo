package com.javaonlinecourse.b1lesson1.homework;

/**
 * Преобразовать формулы в код. Формулы даны в Slack
 */
public class HW08 {
    public static void main(String[] args) {
        double a, b, y, z, x;

        // a = (1+Math.pow(Math.sin(x+y),2)) / (2+Math.abs(x-2*x/(1+x*x*y*y))) + x;

        //  b = Math.pow(Math.atan(1/z),2);


    }
}
